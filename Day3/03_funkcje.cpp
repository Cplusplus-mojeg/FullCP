#include <iostream>
 int absolut(int x) {
     std::cin >> x;
     if (x < 0) {
        return << -x << "\n";
     }
    else {
        return << x << "\n";
     }
 }
int main() {
    absolut(); 
    return absolut();
}
