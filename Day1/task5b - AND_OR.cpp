#include <iostream>

int main() {
    // AND - &&
   
    std::cout << "(true && true) = " << (true && true) << "\n ";
    
    std::cout << "---------\n ";
    //OR - ||
    //Negation - !
    std::cin >> x;
   
    
    
    if (x >= 10) {
        std::cout << "Jest wieksze lub rowne 10 \n ";
        if (x < 20) {
        std::cout << "Jest w przedziale 10 - 20\n ";
    }
        else { 
        std::cout << "Liczba nie jest w przedziale\n ";
        
    }
}
